extends layout

block content
  h1= title
   - var image_names=["Eagle1.jpg","Eagle2.jpg","Eagle3.jpg","Eagle4.jpg","Eagle5.jpg"]
     - for(let i=0; i<3; i++) {
        - var img= Math.floor(Math.random()*5)
           img(src=`/images/${image_names[img]}` alt=`${img}` width='300px' height='300px')
     - }