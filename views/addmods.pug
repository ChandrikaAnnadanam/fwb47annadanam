extends layout

block content
  h1= title
  p `rows size is #{query.rows}`
  p `cols size is#{query.cols}`
  - var rows=Number(query.rows)
  - var cols=Number(query.cols)
  if !(Number.isInteger(rows)) || !(Number.isInteger(cols))
    p Values are not present
  else if rows > 6
    p row size is large
  else if cols > 6
    p column size is large
  else
   table#outer
    - for(let i=0; i<query.rows; i++) { 
       tr#middle
         - for(let j=0; j<query.cols; j++) { 
           td= (+i + +j) % query.rows
         - }
    - }